export default function QuoteCard() {

    const quote = "Small progress is still progress."
  
    return (
      <div>
        <h3>Quote of the day</h3>
        <p>"{quote}"</p>
      </div>
    )
  
  }